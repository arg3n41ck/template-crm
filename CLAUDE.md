# Agent entrypoint

Follow `AGENTS.md` as the authoritative project contract. Canonical local skills live in `.ai/skills`; `.agents/skills`, `.claude/skills` and `.codex/skills` are compatibility links.

Before coding: identify the relevant module and skill. After coding: run the smallest checks in `.codex-harness/VERIFICATION.md`, then broaden only when the impact requires it.
